# The board, step by step

**Generated by `bunx tsx scripts/_step-wiring.ts`. Do not edit by hand.** Step numbers are positions in
`src/config/delivery-steps.ts`, so inserting a step renumbers every step after it. Read this file before
changing anything about a step: it says what runs it, what its [Done] checks, and what its thread accepts.

## Re-running a step

| where | what to type | what happens |
| --- | --- | --- |
| in a step's thread | `rerun` | that step runs again, its card updates in place, notes land in the same thread |
| anywhere in the client's channel | `rerun step 13` | step 13 runs again with a NEW card at the bottom of the channel |
| anywhere in the client's channel | `rerun 18-21` | each step in the range, in order, each with a new card at the bottom |

The old thread is kept and gets one line linking to the new card. A re-run clears the tick, the verification and
any error, then runs the step's runner; it never deletes a decision a person made (approved keywords, a picked
anchor, a frozen `custom_v1`). Steps with no runner just get their card again.

Behind it: `src/lib/clients/step-rerun.ts` and `/api/internal/rerun-steps` (one step per request, because a
range is minutes of work).

## Every step

| # | key | label | phase | mode | runner | [Done] checks | card body | what its thread takes | waits on |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `intake_received` | Intake received, canonical NAP locked, audit attached if one exists | Before the call | auto | none | a system check | default | Done / Skip / I hit a problem | nothing |
| 2 | `baseline_scan` | Pre-call audit attached (the one that got them to book) | Before the call | auto | none | a system check | default | Done / Skip / I hit a problem | `intake_received` |
| 3 | `site_dns_intel` | Site, hosting and DNS intelligence | Before the call | auto | `runSiteIntel()` | a system check | default | Done / Skip / I hit a problem | `intake_received` |
| 4 | `nap_sweep` | Presence sweep: automated tier | Before the call | auto | `runAutomatedSweep()` | a system check | default | Done / Skip / I hit a problem | `intake_received` |
| 5 | `presence_sweep_manual` | Presence sweep: manual tier, screenshots in the thread | Before the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `nap_sweep` |
| 6 | `competitor_shortlist` | Competitor shortlist of 10, top 3 pre-picked, I confirm | Before the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `baseline_scan` |
| 7 | `avatar_confirmed` | Avatar confirmed: which customer this whole build is aimed at | Before the call | manual | none | a system check | yes | `avatar: <label>`<br>[Avatar] buttons | `baseline_scan` |
| 8 | `review_audit` | Review audit: them plus the three I picked | Before the call | auto_then_manual | `runReviewAudit()` | a system check | yes | Done / Skip / I hit a problem | `competitor_shortlist` |
| 9 | `offer_proposed` | One offer proposed from what they told us at intake | Before the call | auto | `proposeOffer()` | a system check | default | Done / Skip / I hit a problem | `intake_received`, `avatar_confirmed` |
| 10 | `offer_locked` | Prep call: phone them, lock the one offer and the words their customers use for it | Before the call | manual | none | a system check | yes | `offer:`<br>`terms:`<br>`outcome:`<br>`price:`<br>[Call now]<br>`review platform: <name>`, `review link: <url>` | `avatar_confirmed` |
| 11 | `avatar_harvest` | Buyer-phrase harvest and the deep research for the confirmed avatar | Before the call | auto_then_manual | `runHarvest()` | a system check | yes | `research:` or a dropped file<br>`prompt`, `prompt short`, `run`<br>`avatar sheet:`, `short offer:`, `beliefs:`<br>`share research`, `share sheet` | `baseline_scan`, `avatar_confirmed`, `offer_locked` |
| 12 | `keyword_set` | Keywords: 200+ ways the offer is said, approved by me | Before the call | auto_then_manual | `runKeywordStep()` | a system check | yes | `keywords approve`<br>`keywords drop N`<br>`keywords add:`<br>`keywords more <category>` | `offer_locked`, `avatar_harvest` |
| 13 | `custom_question_set` | Custom question set drafted for approval | Before the call | auto | `generateCustomQuestionSet()` | a system check | yes | `objection: <what they said>`<br>`objection: ... \| belief: <key>` | `avatar_confirmed`, `offer_locked`, `keyword_set` |
| 14 | `page_candidates` | Page candidates scored and ranked for the call | Before the call | auto | `generatePageCandidates()` | an artifact on record | yes | Done / Skip / I hit a problem | `avatar_confirmed`, `offer_locked`, `keyword_set` |
| 15 | `citation_cleanup_list` | Citation cleanup list built and ranked | Before the call | auto | `generateCitationCleanupList()` | an artifact on record | yes | Done / Skip / I hit a problem | `presence_sweep_manual` |
| 16 | `hub_preview` | Hub built, themed, preview live, theme confirmed by me | Before the call | auto_then_manual | `registerHubAndSeedDns()` | a system check | yes | `universe <name>`<br>`template <name>`<br>`skin reset`<br>`pick 1\|2\|3`<br>paste a screenshot | `intake_received` |
| 17 | `referral_engine_preview` | AI Referral Engine preview live, themed to match | Before the call | auto | `verifyReferralEnginePreview()` | a system check | yes | `review link: <url>`<br>[Paste review link]<br>`universe <name>` | `hub_preview` |
| 18 | `concierge_preview` | AI Concierge preview live, ready to demo on the call | Before the call | auto_then_manual | `provisionConcierge()` | a system check | yes | [Include concierge (add-on)] / [Not now, install later]<br>[Patient lane] / [Owner lane]<br>`concierge install`<br>[Character menu] / [Skip, keep the default]<br>`mascot`, `mascot concepts`<br>`mascot pick a, b, c`, `mascot <key>`, `mascot skip`<br>`mascot corner bottom-left`<br>paste art with `mascot <key> <state>` | `hub_preview` |
| 19 | `site_replica` | Replica of their own site built, assistant on it, preview link ready to walk | Before the call | auto_then_manual | `buildSiteReplica()` | a system check | yes | `universe <name>`<br>paste a screenshot | `hub_preview`, `concierge_preview` |
| 20 | `review_card_pdf` | Review card PDF generated | Before the call | auto | `generateReviewCard()` | an artifact on record | yes | `review link: <url>`<br>[Paste review link] | `hub_preview` |
| 21 | `pre_call_pages` | Seven pages drafted before the call: one pillar for the offer, six supports | Before the call | auto_then_manual | `runPreCallPlan()` | a system check | yes | `ladder`, `ladder pick <1-5>`, `anchor at <1-5>`, `ladder problem aware`<br>`headlines`, `headlines pick 4, 9, 12, ...` (seven)<br>`emotional:` then one question per line<br>[Anchor at N], [Pillar N], [Supports: pick for me]<br>`pillar: <rank>` / `pillar: auto`<br>`supports: 3, 7, 12` / `supports auto`<br>`guarantee:`, `outcome:`, `price:`<br>`plan`, `plan new`, `plan approve`, `plan swap N`, `plan drop N`, `plan edit N: ...`<br>`anchor`, `anchor: <key>`<br>`research:` (page batch)<br>`headline N`, `skeleton N` | `offer_locked`, `keyword_set`, `page_candidates`, `concierge_preview` |
| 22 | `call_sheet` | Call pack: call sheet, findings, presence PDF, closing questions | Before the call | auto | `run()` | a system check | default | Done / Skip / I hit a problem | `presence_sweep_manual`, `review_audit`, `custom_question_set`, `page_candidates`, `hub_preview`, `pre_call_pages` |
| 23 | `call_booked` | Call booked | During the call | manual | none | a system check | yes | Done / Skip / I hit a problem | nothing |
| 24 | `call_held` | Call held: NAP aloud, question set approved, consent confirmed, preview and the drafted pages walked | During the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `call_sheet` |
| 25 | `access_granted` | Access granted: GBP manager, Search Console, Analytics | During the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `call_held` |
| 26 | `dns_records` | DNS: three records added by the client, two CNAMEs and one TXT | During the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `call_held` |
| 27 | `agreement_signed` | Agreement signed on the call | During the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `call_held` |
| 28 | `day_zero_archive` | Day-0 scan archived, before any change lands | After the call | manual | none | a system check | default | Done / Skip / I hit a problem | `call_held` |
| 29 | `gbp_buildout` | Google Business Profile buildout: categories, services, photos, Q&A seeded | After the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `day_zero_archive`, `access_granted` |
| 30 | `citation_cleanup` | Citation cleanup executed from the list | After the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `day_zero_archive`, `citation_cleanup_list` |
| 31 | `subdomain_live` | Subdomain live and verified in Search Console | After the call | auto_then_manual | `checkHubResolving()` | a system check | yes | Done / Skip / I hit a problem | `dns_records` |
| 32 | `first_page` | First pages published, measured track first | After the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `day_zero_archive`, `subdomain_live` |
| 33 | `cards_printed` | Cards printed and handed to the clinic | After the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `review_card_pdf` |
| 34 | `review_request_configured` | Automated request configured in their booking system, or card_only recorded | After the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `call_held` |
| 35 | `referral_engine_handed` | AI Referral Engine handed to the named person | After the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `subdomain_live` |
| 36 | `concierge_live` | AI Concierge enabled: audience confirmed, booking destination set, consent copy approved | After the call | manual | none | a system check | yes | `concierge install` | `subdomain_live`, `call_held` |
| 37 | `tracking_installed` | SRT pixel live on the client site, first real session seen | After the call | auto_then_manual | `provisionPixelKey()` | a system check | yes | Done / Skip / I hit a problem | `call_held` |
| 38 | `self_report_field` | How did you hear about us: six options live on their own booking form | After the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `call_held` |
| 39 | `time_log_entries` | Time log has entries from day 0 | After the call | auto | none | a system check | default | Done / Skip / I hit a problem | `day_zero_archive` |
| 40 | `weekly_report` | Weekly report firing | After the call | auto | none | a system check | default | Done / Skip / I hit a problem | `first_page` |
| 41 | `day_30_date` | Day-30 report date set | After the call | manual | none | a system check | yes | Done / Skip / I hit a problem | `day_zero_archive` |

## The files a step touches

- **The list itself:** `src/config/delivery-steps.ts` (order, phase, mode, blockedBy). `stepNumber()` is the
  only honest source of a step's number.
- **Runners:** `src/lib/clients/artifacts/registry.ts` maps a step key to the function that generates it.
- **[Done]:** `src/lib/clients/step-verify.ts`, one verifier per step key, and the build fails without one.
- **Cards:** `src/lib/clients/step-engine.ts` (`instructionsFor` for the body, `extraActionsFor` for buttons).
- **Threads:** `src/app/api/slack/events/route.ts` dispatches to each step's handler; a command typed in the
  wrong thread gets a pointer from `src/lib/clients/step-commands.ts`.
- **The board in Slack:** `src/lib/clients/step-board.ts` (anchors, marks, `notifyStep`).
