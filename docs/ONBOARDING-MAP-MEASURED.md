# The onboarding map, measured 2026-09-17 against `srt-agency-llc`

**Generated by `bunx tsx --env-file=.env.local scripts/_step-wiring.ts --live`. Do not edit by hand, and do not add it to the drift check:** every number here changes when production does.

Its static companion is `docs/ONBOARDING-MAP.md`, which says what each step needs and would ask. This file says what this one client actually holds.

Client: **SRT Agency LLC**, https://srtagency.com, vertical `aeo-agency-med-spa`.

## What this client holds

| table | rows | why it matters | the select |
| --- | --- | --- | --- |
| `client_delivery_steps` | 41 | the board itself | `select client_id from client_delivery_steps where client_id = <this client> limit 1000` |
| `client_audiences` | 1 | who we are selling to | `select client_id from client_audiences where client_id = <this client> limit 1000` |
| `client_offers` | 1 | what is being sold | `select client_id from client_offers where client_id = <this client> limit 1000` |
| `audience_documents` | 2 | the framework documents: sheet, offer, beliefs, ladder | `select client_id from audience_documents where client_id = <this client> limit 1000` |
| `avatar_briefs` | 0 | shared research per avatar, keyed on the avatar and NOT on this client | `select first_client_id from avatar_briefs where first_client_id = <this client> limit 1000` |
| `client_keywords` | 385 | the searches the pages answer | `select client_id from client_keywords where client_id = <this client> limit 1000` |
| `client_headlines` | 33 | the headline each page promises | `select client_id from client_headlines where client_id = <this client> limit 1000` |
| `page_plan` | 0 | the planned pages | `select client_id from page_plan where client_id = <this client> limit 1000` |
| `page_angles` | 0 | the idea behind each page | `select client_id from page_angles where client_id = <this client> limit 1000` |
| `client_pages` | 0 | pages that have actually been drafted | `select client_id from client_pages where client_id = <this client> limit 1000` |
| `page_dataset` | 0 | the training record of what was drafted | `select client_id from page_dataset where client_id = <this client> limit 1000` |
| `page_plan_runs` | 0 | snapshots taken before a plan is replaced | `select client_id from page_plan_runs where client_id = <this client> limit 1000` |
| `page_sources` | 7 | the evidence a page is allowed to cite | `select client_id from page_sources where client_id = <this client> limit 1000` |
| `page_gate_runs` | 0 | quality gate verdicts | `select client_id from page_gate_runs where client_id = <this client> limit 1000` |
| `lead_magnets` | 1 | what the concierge hands over | `select client_id from lead_magnets where client_id = <this client> limit 1000` |
| `client_datasets` | 0 | every paid pull we hold for this lead | `select client_id from client_datasets where client_id = <this client> limit 1000` |
| `client_events` | 145 | what has happened in this client's channel | `select client_id from client_events where client_id = <this client> limit 1000` |

## The board, as it stands

Cursor: **11. `avatar_harvest`**.

| # | step | status | verified by | completed |
| --- | --- | --- | --- | --- |
| 1 | `intake_received` | complete | system | 2026-09-15 |
| 2 | `baseline_scan` | complete | system | 2026-09-15 |
| 3 | `site_dns_intel` | complete | system | 2026-09-15 |
| 4 | `nap_sweep` | complete | system | 2026-09-15 |
| 5 | `presence_sweep_manual` | skipped | not verified | 2026-09-15 |
| 6 | `competitor_shortlist` | complete | system | 2026-09-15 |
| 7 | `avatar_confirmed` | complete | system | 2026-09-15 |
| 8 | `review_audit` | skipped | not verified | 2026-09-15 |
| 9 | `offer_proposed` | complete | system | 2026-09-15 |
| 10 | `offer_locked` | complete | system | 2026-09-15 |
| 11 | `avatar_harvest` | awaiting_me | not verified | no |
| 12 | `keyword_set` | complete | system | 2026-09-15 |
| 13 | `custom_question_set` | complete | system | 2026-09-16 |
| 14 | `page_candidates` | complete | system | 2026-09-15 |
| 15 | `citation_cleanup_list` | complete | system | 2026-09-15 |
| 16 | `hub_preview` | complete | system | 2026-09-15 |
| 17 | `referral_engine_preview` | complete | system | 2026-09-15 |
| 18 | `concierge_preview` | complete | system | 2026-09-16 |
| 19 | `site_replica` | complete | system | 2026-09-16 |
| 20 | `review_card_pdf` | running | not verified | no |
| 21 | `pre_call_pages` | awaiting_me | not verified | no |
| 22 | `call_sheet` | pending | not verified | no |
| 23 | `call_booked` | pending | not verified | no |
| 24 | `call_held` | pending | not verified | no |
| 25 | `access_granted` | pending | not verified | no |
| 26 | `dns_records` | pending | not verified | no |
| 27 | `agreement_signed` | pending | not verified | no |
| 28 | `day_zero_archive` | pending | not verified | no |
| 29 | `gbp_buildout` | pending | not verified | no |
| 30 | `citation_cleanup` | pending | not verified | no |
| 31 | `subdomain_live` | pending | not verified | no |
| 32 | `first_page` | pending | not verified | no |
| 33 | `cards_printed` | pending | not verified | no |
| 34 | `review_request_configured` | pending | not verified | no |
| 35 | `referral_engine_handed` | pending | not verified | no |
| 36 | `concierge_live` | pending | not verified | no |
| 37 | `tracking_installed` | pending | not verified | no |
| 38 | `self_report_field` | pending | not verified | no |
| 39 | `time_log_entries` | pending | not verified | no |
| 40 | `weekly_report` | pending | not verified | no |
| 41 | `day_30_date` | pending | not verified | no |

## What each step is still missing, on this client

This is `gapLines()` against the real datasets, uncapped. A step that asks for nothing says so.

### 1. `intake_received`

```
Step 1 can complete. 1 thing would make it better.
:warning: the client's own customer reviews
    reviews filed as CUSTOMER_REVIEW evidence, on intake_received
```

### 2. `baseline_scan`

```
*Step 2* asks for nothing from the datasets: the audit engine fires and scores it; no answer from a person fills a dataset field here.
```

### 3. `site_dns_intel`

```
*Step 3* asks for nothing from the datasets: hosting, DNS and the site are observed from the network, never answered.
```

### 4. `nap_sweep`

```
*Step 4* asks for nothing from the datasets: the automated tier reads directories; nothing in the datasets feeds or records it.
```

### 5. `presence_sweep_manual`

```
*Step 5* asks for nothing from the datasets: screenshots in the thread, filed as evidence against client_docs rather than as dataset fields.
```

### 6. `competitor_shortlist`

```
*Step 6* asks for nothing from the datasets: the shortlist comes from who the engines named in the audit, not from anything we ask for.
```

### 7. `avatar_confirmed`

```
:white_check_mark: *Step 7* has everything it needs on file.
```

### 8. `review_audit`

```
*Step 8* asks for nothing from the datasets: their reviews and three competitors' are read from live listings; no dataset field is owed first.
```

### 9. `offer_proposed`

```
*Step 9* asks for nothing from the datasets: the proposal is written from what intake already said, so nothing has to be collected for it.
```

### 10. `offer_locked`

```
Step 10 needs 2 more things before it can complete.
:no_entry: the outcome promised ("more appointments", "more jobs")
    → `outcome: more appointments` (its own message), on offer_locked
:no_entry: the price or ticket
    → `price: $399 per session` (its own message), on offer_locked
:warning: how they want it positioned
    → `offer: <name> | <positioning>`, on offer_locked
:warning: the approved sales letter
    → `letter use`, `letter draft` or `letter replace:`, then `letter approve`, on offer_locked
:warning: 2 fields: the customer picked for the Loom, best and worst customer map
    → `loom` on the linked audit, then pick a customer
```

### 11. `avatar_harvest`

```
Step 11 needs 3 more things before it can complete.
:no_entry: 20 fields: age range, gender split, where they live, and more
    → `avatar sheet:` in step 11's thread (message 4)
:no_entry: 14 fields: the short offer, the big idea, the metaphor, and more
    → `short offer:` in step 11's thread (message 5)
:no_entry: the necessary beliefs (up to 6, "I believe that")
    → `beliefs:` in step 11's thread (message 7 of the framework script)
:warning: 7 fields: hopes and dreams, beyond the product, victories and failures around the problem, prejudices, and more
    only step 11's framework script asks for this
:warning: 5 fields: the cost of doing nothing, who else weighs in on the decision, the proof they need before buying, and more
    nothing asks for this yet
```

### 12. `keyword_set`

```
:white_check_mark: *Step 12* has everything it needs on file.
```

### 13. `custom_question_set`

```
:white_check_mark: *Step 13* has everything it needs on file.
```

### 14. `page_candidates`

```
:white_check_mark: *Step 14* has everything it needs on file.
```

### 15. `citation_cleanup_list`

```
*Step 15* asks for nothing from the datasets: the list is built from the directories the sweep found, and is ranked by what it measured.
```

### 16. `hub_preview`

```
*Step 16* asks for nothing from the datasets: the hub is built from the client's own theme and pages; its look is picked, not answered.
```

### 17. `referral_engine_preview`

```
*Step 17* asks for nothing from the datasets: the AI Referral Engine mirrors listings that already exist and asks for no dataset field.
```

### 18. `concierge_preview`

```
:white_check_mark: *Step 18* has everything it needs on file.
```

### 19. `site_replica`

```
*Step 19* asks for nothing from the datasets: the replica is a crawl of their own site, filed as evidence rather than collected as fields.
```

### 20. `review_card_pdf`

```
*Step 20* asks for nothing from the datasets: the card is rendered from the review destination already on the client row.
```

### 21. `pre_call_pages`

```
Step 21 needs 1 more thing before it can complete.
:no_entry: the necessary beliefs (up to 6, "I believe that")
    → `beliefs:` in step 11's thread (message 7 of the framework script)
:warning: 3 fields: the big idea, objections to buying, the belief chains
    → `short offer:` in step 11's thread (message 5)
:warning: the outcome promised ("more appointments", "more jobs")
    → `outcome: more appointments` (its own message), on offer_locked
:warning: awareness stage (5 to 1, with quotes)
    only step 11's framework script asks for this
:warning: fears and deep frustrations
    → `avatar sheet:` in step 11's thread (message 4)
```

### 22. `call_sheet`

```
Step 22 can complete. 4 things would make it better.
:warning: 2 fields: the customer picked for the Loom, best and worst customer map
    → `loom` on the linked audit, then pick a customer
:warning: the outcome promised ("more appointments", "more jobs")
    → `outcome: more appointments` (its own message), on offer_locked
:warning: the price or ticket
    → `price: $399 per session` (its own message), on offer_locked
:warning: objections to buying
    → `short offer:` in step 11's thread (message 5)
```

### 23. `call_booked`

```
*Step 23* asks for nothing from the datasets: a date in the calendar; nothing about the client is collected by it.
```

### 24. `call_held`

```
*Step 24* asks for nothing from the datasets: the call itself. What it captures is written by offer_locked, not here.
```

### 25. `access_granted`

```
*Step 25* asks for nothing from the datasets: GBP, Search Console and Analytics access is granted, never answered.
```

### 26. `dns_records`

```
*Step 26* asks for nothing from the datasets: three records the client adds, verified by observation.
```

### 27. `agreement_signed`

```
*Step 27* asks for nothing from the datasets: a signature on a contract, recorded against the signing row.
```

### 28. `day_zero_archive`

```
*Step 28* asks for nothing from the datasets: the before photograph. It archives what was already measured and asks for nothing.
```

### 29. `gbp_buildout`

```
*Step 29* asks for nothing from the datasets: categories, services and photos are entered in Google, not here.
```

### 30. `citation_cleanup`

```
*Step 30* asks for nothing from the datasets: the list built at step 15 is executed; no new field is owed.
```

### 31. `subdomain_live`

```
*Step 31* asks for nothing from the datasets: DNS and Search Console verification, both observed.
```

### 32. `first_page`

```
*Step 32* asks for nothing from the datasets: the pages were drafted and gated at step 21; publishing them collects nothing new.
```

### 33. `cards_printed`

```
*Step 33* asks for nothing from the datasets: a physical deliverable handed over in person.
```

### 34. `review_request_configured`

```
*Step 34* asks for nothing from the datasets: a setting in their booking system, or the printed cards.
```

### 35. `referral_engine_handed`

```
*Step 35* asks for nothing from the datasets: a named person is given the tool; the naming is not a dataset field.
```

### 36. `concierge_live`

```
*Step 36* asks for nothing from the datasets: the switch, the booking destination and the audience were all confirmed at concierge_preview.
```

### 37. `tracking_installed`

```
*Step 37* asks for nothing from the datasets: the pixel is live or it is not, and a real session proves it.
```

### 38. `self_report_field`

```
*Step 38* asks for nothing from the datasets: six options added to their own booking form.
```

### 39. `time_log_entries`

```
*Step 39* asks for nothing from the datasets: hours recorded as work happens.
```

### 40. `weekly_report`

```
*Step 40* asks for nothing from the datasets: the report is assembled from what was measured. ATTRIBUTION_NOT_WIRED says in writing what it cannot count.
```

### 41. `day_30_date`

```
*Step 41* asks for nothing from the datasets: a date for the day-30 retest.
```

